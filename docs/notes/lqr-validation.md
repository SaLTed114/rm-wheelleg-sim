# LQR 参数生成与验证

本文由 `tools/lqr/build_current_model.py` 自动生成。

## 旧实车参数 Golden Test

- 固件系数最大误差：`4.974874e-10`。
- 原始增益闭环特征值模最大值：`0.995096731`。
- 离散 Riccati 方程最大残差：`2.026490e-12`。
- 结论：Python 生成器能够数值复现当前实车固件中的参数表。

## 偏航惯量结论

将 `equ7` 代入 `equ5` 后，所有偏航惯量项严格化简为
`-I_z * D2psi`。原始推导也通过
`I * D2psi = R_l * (-N_f,l + N_f,r)` 将 `I` 定义为完整的机体偏航惯量，
方程中没有遗留的二分之一系数。

- `base_link` 实际 Izz：`3.047199970 kg*m^2`。
- 正式模型倍率：`1.0x`。
- 敏感性对照倍率：`2.0x`，对应
  `6.094399941 kg*m^2`。
- 两种情况的原始增益最大差：
  `1.178199615`。

正式调度采用一倍实际惯量。两倍惯量只保留为敏感性对照，不能在其他位置
再次乘二。

## 当前模型参数

- 左右共同安全腿长范围：`0.186` 至
  `0.390 m`。
- 控制周期：`1.0 ms`。
- 机体质量：`11.000000 kg`。
- 机体俯仰惯量：`2.873599847 kg*m^2`。
- 机体偏航惯量：`3.047199970 kg*m^2`。
- 单轮质量：`0.900000 kg`；半径：
  `0.058000003 m`；轴向惯量：
  `0.000437158 kg*m^2`。
- 半轮距：`0.207750000 m`；单腿质量：
  `3.100000 kg`。
- 降阶前后质量分配误差：`3.553e-15 kg`。
- 机体质心相对虚拟髋关节的高度为
  `0.071412000 m`，前向偏移为
  `-0.000525600 m`。

源 XML 中物理右腿的闭链连接点存在约 1.95 mm 的固定横向错位。因此可行性
判断采用每侧固有闭合误差加 0.5 mm，而不是统一使用 0.5 mm 绝对阈值；采样
得到的最大闭合误差为
`1.950000 mm`。

实际腿部质心明显偏离髋关节到轮轴的连线：

- 物理左腿垂直于虚拟腿轴的最大偏移：
  `65.850 mm`。
- 物理右腿垂直于虚拟腿轴的最大偏移：
  `64.904 mm`。

降阶惯量使用 `I_leg = I_about_hip - m_leg * L_b^2` 保留该偏置造成的
惯量。轮子没有参与腿部聚合，因为原动力学方程已经单独包含轮子质量和惯量。

## 当前增益调度验证

- 多项式阶数：`3`。
- 所有采样点的可控矩阵秩：`10`。
- 原始增益闭环特征值模最大值：
  `0.997892384`。
- 拟合增益在 201 个腿长点上的闭环特征值模最大值：
  `0.997891375`。
- 离散 Riccati 方程最大残差：`1.904782e-12`。
- 增益拟合最大误差：`1.054039e-02`。
- float Horner 求值最大误差：
  `1.378685e-06`。

当前模型的第一版调度有意保留旧实车 Q/R 权重，只改变物理参数。它按照用户
指定的 1 ms 仿真控制周期生成，不能直接用于现有 3 ms 实车控制循环。
